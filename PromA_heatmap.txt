library(seqinr)

# Read a fasta file
seqs <- read.fasta(file="41PromA.fasta", seqtype="DNA", strip.desc=TRUE)

# dinucleotide relative abundance
myrho <- sapply(seqs, rho, wordsize = 3)

# Draw a Heat Map
heatmap(myrho, margins=c(7, 2), cexCol=0.2, scale="none", col= heat.colors(12))

 


